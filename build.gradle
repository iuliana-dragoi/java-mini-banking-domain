plugins {
    id 'java'
    id 'application'
}

group = 'com.example'
version = '1.0.0'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

application {
    mainClass = 'com.example.bank.Main'
}

repositories {
    mavenCentral()
}

dependencies {
    // nimic deocamdată
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}